name: Release

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version_name:
        description: "Version name (e.g. 0.1.1)"
        required: true
        default: ''

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.resolve.outputs.version_name }}
      release_tag: ${{ steps.resolve.outputs.release_tag }}
    steps:
      - name: Resolve version
        id: resolve
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            TAG="${GITHUB_REF#refs/tags/}"
            echo "version_name=${TAG}" >> "$GITHUB_OUTPUT"
            echo "release_tag=${TAG}" >> "$GITHUB_OUTPUT"
          else
            VN="${{ inputs.version_name }}"
            if [ -z "${VN}" ]; then
              echo "::error::version_name is required for manual dispatch"
              exit 1
            fi
            echo "version_name=${VN}" >> "$GITHUB_OUTPUT"
            case "${VN}" in
              v*) echo "release_tag=${VN}" >> "$GITHUB_OUTPUT" ;;
              *)  echo "release_tag=v${VN}" >> "$GITHUB_OUTPUT" ;;
            esac
          fi

  build:
    needs: version
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"
          cache: "gradle"
      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
      - name: CI Gradle tuning
        run: |
          sed -i 's|^org.gradle.jvmargs.*|org.gradle.jvmargs=-Xmx6g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8|' gradle.properties
          sed -i 's|^kotlin.daemon.jvmargs.*|kotlin.daemon.jvmargs=-Xmx4g -XX:+UseG1GC|' gradle.properties
          sed -i 's|^org.gradle.workers.max.*|org.gradle.workers.max=1|' gradle.properties
          grep -q '^org.gradle.parallel=' gradle.properties || printf '\norg.gradle.parallel=false\n' >> gradle.properties
          grep -q '^kotlin.compiler.execution.strategy=' gradle.properties || printf '\nkotlin.compiler.execution.strategy=in-process\n' >> gradle.properties
      - name: Cache Gradle configuration cache
        uses: actions/cache@v4
        with:
          path: .gradle/configuration-cache
          key: config-cache-v2-${{ runner.os }}-${{ hashFiles('gradle.properties', '*.gradle.kts', '**/*.gradle.kts', 'buildSrc/**', 'gradle/libs.versions.toml') }}
          restore-keys: config-cache-v2-${{ runner.os }}-
      - name: Generate keystore
        run: |
          mkdir -p keystore
          keytool -genkeypair -v -keystore keystore/release.keystore -alias release -keyalg RSA -keysize 2048 -validity 10000 -storetype PKCS12 -storepass android -keypass android -dname "CN=BLBL, OU=Dev, O=BLBL, L=Shanghai, C=CN"
      - name: Build APK
        env:
          RELEASE_STORE_PASSWORD: android
          RELEASE_KEY_ALIAS: release
          RELEASE_KEY_PASSWORD: android
        run: ./gradlew assembleRelease -PversionName=${{ needs.version.outputs.version_name }} -PversionCode=${{ github.run_number }} --no-daemon --stacktrace
      - name: Copy APKs
        run: |
          mkdir -p output
          cp app/build/outputs/apk/kitkat/release/app-kitkat-release.apk output/app-release.apk
          cp app/build/outputs/apk/kitkat/release/app-kitkat-release.apk "output/blbl-tv-${{ needs.version.outputs.version_name }}.apk"
      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: output/app-release.apk
      - name: Upload TV APK
        uses: actions/upload-artifact@v4
        with:
          name: blbl-tv-apk
          path: "output/blbl-tv-${{ needs.version.outputs.version_name }}.apk"

  publish:
    needs: [version, build]
    if: ${{ always() && !cancelled() && needs.version.result == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download release APK
        uses: actions/download-artifact@v4
        with:
          name: app-release-apk
          path: artifacts/release
        continue-on-error: true

      - name: Download TV APK
        uses: actions/download-artifact@v4
        with:
          name: blbl-tv-apk
          path: artifacts/tv
        continue-on-error: true

      - name: Collect APKs
        id: collect
        run: |
          APK_FILES=""
          VN="${{ needs.version.outputs.version_name }}"
          if [ -f artifacts/release/output/app-release.apk ]; then
            cp artifacts/release/output/app-release.apk "blbl-${VN}-release.apk"
            APK_FILES="$APK_FILES blbl-${VN}-release.apk"
          fi
          if ls artifacts/tv/*.apk 1>/dev/null 2>&1; then
            cp artifacts/tv/*.apk "blbl-tv-${VN}.apk"
            APK_FILES="$APK_FILES blbl-tv-${VN}.apk"
          fi
          echo "apk_files=${APK_FILES}" >> "$GITHUB_OUTPUT"
          echo "Built: $APK_FILES"

      - name: Create/Update GitHub Release
        if: steps.collect.outputs.apk_files != ''
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.version.outputs.release_tag }}
        run: |
          APK_LIST="${{ steps.collect.outputs.apk_files }}"
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            echo "Release $RELEASE_TAG exists, appending APKs..."
            gh release upload "$RELEASE_TAG" $APK_LIST --clobber
          else
            echo "Creating release $RELEASE_TAG..."
            gh release create "$RELEASE_TAG" $APK_LIST --title "$RELEASE_TAG" --generate-notes --target "$GITHUB_SHA"
          fi

      - name: Upload to R2
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID || vars.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BUCKET || vars.R2_BUCKET }}
          R2_PREFIX: ${{ vars.R2_PREFIX || secrets.R2_PREFIX }}
          VERSION_NAME: ${{ needs.version.outputs.version_name }}
        run: |
          set -euo pipefail
          if [ -z "${R2_ACCESS_KEY_ID:-}" ] || [ -z "${R2_SECRET_ACCESS_KEY:-}" ] || [ -z "${R2_ACCOUNT_ID:-}" ] || [ -z "${R2_BUCKET:-}" ]; then
            echo "R2 not configured, skipping upload."
            exit 0
          fi
          if ! command -v aws >/dev/null 2>&1; then
            sudo apt-get update -qq && sudo apt-get install -y awscli
          fi
          export AWS_ACCESS_KEY_ID="${R2_ACCESS_KEY_ID}"
          export AWS_SECRET_ACCESS_KEY="${R2_SECRET_ACCESS_KEY}"
          export AWS_REGION="auto"
          export AWS_EC2_METADATA_DISABLED=true
          ENDPOINT="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"
          CLEAN="${VERSION_NAME#v}"
          PREFIX="${R2_PREFIX:-blbl}"
          PREFIX="${PREFIX%/}"
          [ -n "${PREFIX}" ] && PREFIX="${PREFIX}/"

          if [ -s CHANGELOG.md ]; then
            printf '%s' "${CLEAN}" > version
            aws s3 cp "version" "s3://${R2_BUCKET}/${PREFIX}version" --endpoint-url "${ENDPOINT}" --content-type "text/plain" --cache-control "no-cache"
            aws s3 cp "CHANGELOG.md" "s3://${R2_BUCKET}/${PREFIX}CHANGELOG.md" --endpoint-url "${ENDPOINT}" --content-type "text/markdown; charset=utf-8" --cache-control "no-cache"
          fi
          if [ -f artifacts/release/output/app-release.apk ]; then
            aws s3 cp "artifacts/release/output/app-release.apk" "s3://${R2_BUCKET}/${PREFIX}blbl-${CLEAN}-release.apk" --endpoint-url "${ENDPOINT}" --content-type "application/vnd.android.package-archive"
            aws s3 cp "artifacts/release/output/app-release.apk" "s3://${R2_BUCKET}/${PREFIX}blbl-latest-release.apk" --endpoint-url "${ENDPOINT}" --content-type "application/vnd.android.package-archive" --cache-control "no-cache"
          fi
          echo "R2 upload done"
